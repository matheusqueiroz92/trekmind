# Database (PostgreSQL + pgvector)
# Use with Docker: docker compose up -d
DATABASE_URL=postgresql://trekmind:trekmind@localhost:5432/trekmind

# Auth (use a long random string in production)
JWT_SECRET=your-jwt-secret-change-in-production

# Better Auth (sessions, OAuth, magic link, 2FA)
BETTER_AUTH_SECRET=your-better-auth-secret-min-32-chars
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# OAuth (optional; leave empty to hide social buttons)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=

# OpenAI (for travel agent chat)
OPENAI_API_KEY=sk-...

# Mobile app: API base URL (use your machine IP for device/emulator)
# EXPO_PUBLIC_API_URL=http://localhost:3000
