# Database (PostgreSQL + pgvector)
# Use with Docker: docker compose up -d
DATABASE_URL=postgresql://trekmind:trekmind@localhost:5432/trekmind

# Auth (use a long random string in production)
JWT_SECRET=your-jwt-secret-change-in-production

# Better Auth (sessions, OAuth, magic link, 2FA)
BETTER_AUTH_SECRET=your-better-auth-secret-min-32-chars
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Magic link e-mail (optional; if set, sends real emails via Resend)
RESEND_API_KEY=your-secret-key
RESEND_FROM=TrekMind <noreply@yourdomain.com>

# OAuth (optional; leave empty to hide social buttons)
GOOGLE_CLIENT_ID=your-secret-key
GOOGLE_CLIENT_SECRET=your-secret-key
GITHUB_CLIENT_ID=your-secret-key
GITHUB_CLIENT_SECRET=your-secret-key

# OpenAI (for travel agent chat)
OPENAI_API_KEY=sk-...

# Google Places API (optional; for restaurants, lodging, tourist attractions with photos)
# Enable Places API (New) at https://console.cloud.google.com/
GOOGLE_PLACES_API_KEY=

# Google Maps JavaScript API (optional; for interactive maps - requires Maps JavaScript API)
# When set, uses Google Maps instead of OpenStreetMap/Leaflet
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Mobile app: API base URL (use your machine IP for device/emulator)
EXPO_PUBLIC_API_URL=http://localhost:3000
